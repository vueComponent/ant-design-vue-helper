{
	"a-tree/blockNode": {
		"description": "-",
		"optionType": "boolean",
		"defaultValue": "-"
	},
	"a-tree/selectable": {
		"description": "-",
		"optionType": "boolean",
		"defaultValue": "-"
	},
	"a-tree/treeData": {
		"description": "treeNode of tree",
		"optionType": "TreeNode[]",
		"defaultValue": "-"
	},
	"a-tree/replaceFields": {
		"description": "-",
		"optionType": "{ children?: string; title?: string; key?: string; }",
		"defaultValue": "-"
	},
	"a-tree/autoExpandParent": {
		"description": "Whether to automatically expand a parent treeNode",
		"optionType": "boolean",
		"defaultValue": "true"
	},
	"a-tree/checkable": {
		"description": "Adds a Checkbox before the treeNodes",
		"optionType": "boolean",
		"defaultValue": "false"
	},
	"a-tree/checkedKeys": {
		"description": "(Controlled) Specifies the keys of the checked treeNodes\n(PS: When this specifies the key of a treeNode which is also a parent treeNode,\nall the children treeNodes of will be checked; and vice versa,\nwhen it specifies the key of a treeNode which is a child treeNode,\nits parent treeNode will also be checked. When checkable and checkStrictly is true,\nits object has checked and halfChecked property. Regardless of whether the child or parent treeNode is checked,\nthey won't impact each other.",
		"optionType": "string[] | number[] | { checked: string[]; halfChecked: string[]; }",
		"defaultValue": "[]"
	},
	"a-tree/checkStrictly": {
		"description": "Check treeNode precisely; parent treeNode and children treeNodes are not associated",
		"optionType": "boolean",
		"defaultValue": "false"
	},
	"a-tree/defaultCheckedKeys": {
		"description": "Specifies the keys of the default checked treeNodes",
		"optionType": "string[] | number[]",
		"defaultValue": "[]"
	},
	"a-tree/defaultExpandAll": {
		"description": "Whether to expand all treeNodes by default",
		"optionType": "boolean",
		"defaultValue": "false"
	},
	"a-tree/defaultExpandedKeys": {
		"description": "Specify the keys of the default expanded treeNodes",
		"optionType": "string[] | number[]",
		"defaultValue": "[]"
	},
	"a-tree/defaultExpandParent": {
		"description": "auto expand parent treeNodes when init",
		"optionType": "boolean",
		"defaultValue": "true"
	},
	"a-tree/defaultSelectedKeys": {
		"description": "Specifies the keys of the default selected treeNodes",
		"optionType": "string[] | number[]",
		"defaultValue": "[]"
	},
	"a-tree/disabled": {
		"description": "whether disabled the tree",
		"optionType": "boolean",
		"defaultValue": "false"
	},
	"a-tree/draggable": {
		"description": "Specifies whether this Tree is draggable (IE > 8)",
		"optionType": "boolean",
		"defaultValue": "false"
	},
	"a-tree/expandedKeys": {
		"description": "(Controlled) Specifies the keys of the expanded treeNodes",
		"optionType": "string[] | number[]",
		"defaultValue": "[]"
	},
	"a-tree/filterTreeNode": {
		"description": "Defines a function to filter (highlight) treeNodes.\nWhen the function returns true, the corresponding treeNode will be highlighted",
		"optionType": "(node: TreeNode) => any",
		"defaultValue": "-"
	},
	"a-tree/loadData": {
		"description": "Load data asynchronously",
		"optionType": "(node: TreeNode) => any",
		"defaultValue": "-"
	},
	"a-tree/loadedKeys": {
		"description": "(Controlled) Set loaded tree nodes. Need work with loadData",
		"optionType": "string[]",
		"defaultValue": "[]"
	},
	"a-tree/multiple": {
		"description": "Allows selecting multiple treeNodes",
		"optionType": "boolean",
		"defaultValue": "false"
	},
	"a-tree/selectedKeys": {
		"description": "(Controlled) Specifies the keys of the selected treeNodes",
		"optionType": "string[] | number[]",
		"defaultValue": "-"
	},
	"a-tree/showIcon": {
		"description": "Shows the icon before a TreeNode's title.\nThere is no default style; you must set a custom style for it if set to true",
		"optionType": "boolean",
		"defaultValue": "false"
	},
	"a-tree/showLine": {
		"description": "Shows a connecting line",
		"optionType": "boolean",
		"defaultValue": "false"
	}
}