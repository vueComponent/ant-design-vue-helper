{
	"a-list-item/actions": {
		"description": "The actions content of list item. If itemLayout is vertical, shows the content on bottom,\notherwise shows content on the far right.",
		"optionType": "VNode[] | slot",
		"defaultValue": "-"
	},
	"a-list-item/extra": {
		"description": "The extra content of list item. If itemLayout is vertical, shows the content on right,\notherwise shows content on the far right.",
		"optionType": "string | slot",
		"defaultValue": "-"
	}
}